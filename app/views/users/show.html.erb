<%  @page_title = "#{@user.first_name}'s profile" %>

<div>
  <p><%= @user.first_name %></p>
  <p><%= @user.last_name %></p>
</div>

<div>
  <%= cl_image_tag(current_user.photo,
      width: 400, height: 300, crop: :fill) %>
</div>
